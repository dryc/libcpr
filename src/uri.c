/* This is free and unencumbered software released into the public domain. */

#include "build.h"

const class_t uri_class = {
  .name    = "uri",
  .super   = NULL,
  .vtable  = NULL, /* abstract class */
};
